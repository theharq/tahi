<h1>Invite Editor</h1>

{{#if showEditForm}}
  <label for="editor">Email</label>
  {{select-2-single value=""
             class="editor-select2"
             name="invite_editor"
             selectionSelected="inviteEditor"
             selectedData=editor
             minimumInputLength=0
             selectedTemplate=selectedTemplate
             resultsTemplate=resultsTemplate
             remoteSource=select2RemoteSource
             placeholder="Please select editor"}}

   <div class="button-primary button--green" {{action "saveEditor"}}>Invite</div>
{{else}}
  {{unbound user-thumbnail editor}}
  {{editor.fullName}} is now the editor.
  <div class="glyphicon glyphicon-trash" {{action "removeEditor"}}></div>
 {{/if}}
